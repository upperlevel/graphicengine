package xyz.upperlevel.ulge.window.event;

import xyz.upperlevel.ulge.window.Window;

public class CursorExitEvent extends CursorEvent {
    public CursorExitEvent(Window window) {
        super(window);
    }
}
