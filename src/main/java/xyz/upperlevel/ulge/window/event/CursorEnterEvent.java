package xyz.upperlevel.ulge.window.event;

import xyz.upperlevel.ulge.window.Window;

public class CursorEnterEvent extends CursorEvent {
    public CursorEnterEvent(Window window) {
        super(window);
    }
}
