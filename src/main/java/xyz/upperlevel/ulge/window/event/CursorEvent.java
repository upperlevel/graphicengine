package xyz.upperlevel.ulge.window.event;

import xyz.upperlevel.ulge.window.Window;

public class CursorEvent extends WindowEvent {
    public CursorEvent(Window window) {
        super(window);
    }
}
